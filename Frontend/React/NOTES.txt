-- create a project in react 
    -- cmd to create new project
        -- npm create vite@latest

-- dependency : a software which helps the application to run.
    1. dependencies 
        -- helps the application to run in development stage and the production stage.
    2. dev dependencies
        -- helps the application to run in development stage.

-- To run an application we have to use a command "npm run dev"
-- It will provide a url for us. copy and paste it inside the browser. 

-- jsx -- javascript + XML 

-- index.html <--- place content

-- load -- render

-- ROOT : location in our html page which will holds the rendered content in the webpage.
-- To create Root we are using createRoot(element)

-- DOM : we select the elements in html page inside the js file. document.querySelector(), getElementById

-- component : If we are having an HTML then we call it as a component.

-- To call a component we use ComponentName as a tag name. 
    <ComponentName />
        (or)
    <ComponentName></ComponentName>

-- Component can't return mutliple tags at a time. so return multiple elements we wrap it with in a tag and return as a single element.
-- When we are returning the elements which are wrapped inside another element. They both are child to the wrapped element, but if you want to have both the child elements act as individual elements without the parent element then we have to use jsx fragment.

jsx frament -- <></> -- dummy tag which will be ignored by the browser when rendering the elements so that when we are returning the elements we are going to wrap it inside the jsx fargment hence we are returning only one element as per jsx but when rendering the elements the browser will ignore the fargment and only considers the child elements.
------------------------------------------------------------------------------------------------------------

PROPS : 
-- To pass information from one component to another component we have to use props (property)
-- Whatever the information we are passing to a component will always be recieved as object.
-- The information that you want to pass to a component will be sent as a attribute.

<Component 
    key1 = "value1"
    key2 = "value2"
/>

{
    key1 : "value1",
    key2 : "value2"
}

function Component(para){

}

accessing the prop : 
para.key1

------------------------------------------------------------------

Handling arrays in React js.

react having special feature to display the arrays directly for example 

const a = ["data1","data2","data3","data4"]

return <div>
    {a}
</div>

output : 
data1data2data3data4

We can use array methods such as filter, map to include the tags to the data.


a.map() --- it will generate a duplicate array

a.map((item) => {
    return <p>{item}</p>
})

// the above operation will return an array of paragraph elements with the data.

------------------------------------------------------------------------------------

state : 

when we load the content on DOM then react it going have a copy on virtual DOM.

State Management Hooks (function) : 
We use this hook to manage the state if a variable.

1. useState()
    -- whenever we use the useState() it will return an array with two values inside it. the array looks as shown below.

    sample : 
        useState() ---> return [initValue, functionToChangeTheValue]

    -- We will use array destructing to separate the data and function
        syntax : 
            const [variableName, setVariableName] =  useState(initialValue)

            -- it will store the initValue inside variableName
            -- it wil store the initValue changing function inside setVariableName
    -- It will re-render the entire component whenever there is a change in it's state.

2. useRef()
    -- Also a state management hook but unlike useState it will not return an array to us. Instead we are going to get an object with a key current
    sample : 
        {current : initvalue}
    -- Whenever you want to update the value of a useRef we have to use the below approach

        syntax : 
            const variable = useRef(initValue); //variable going to hold the object with key current which is holding the initValue. 

            variable.current = updatedValue.

    -- useRef is not capable of re-rendering the component. Instead of re-rendering the component what it will do is to update the value and keep a track of it and when the component re-renders then it is going update the state.